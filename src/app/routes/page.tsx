import React from 'react';

export default function RoutesIndexPage() {
  return (
    <div style={{ padding: 32 }}>
      <h1>Manual Routing Example</h1>
      <p>
        This is the <code>/routes</code> index page. Try visiting <code>/routes/anything</code> to see manual routing in action.
      </p>
    </div>
  );
} 